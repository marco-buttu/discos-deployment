---

- include: observers.yml

- name: Create the /archive directory
  become: true
  file:
    path: "/archive"
    state: directory
    owner: "{{ user }}"
    group: observers
    mode: 0755

- include: lustre.yml
- include: archive_tree.yml

- include: nis.yml
  when: nis_server_ip is defined

- include: guests.yml
