{% for hostname in play_hosts %}
{% if 'ansible_host' in hostvars[hostname] %}
{{ hostvars[hostname].ansible_host }} {{ hostvars[hostname].inventory_hostname }} {{ hostvars[hostname].inventory_hostname_short }}
{% endif %}
{% endfor %}
